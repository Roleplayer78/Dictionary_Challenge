#ifndef _SPELLCHECK_H
#define _SPEELCHECK_H

typedef enum {
    OK = 0,         // Operation succeeded (no errors)
    NOK             // Generic error - Operation failed
}ErrorCodes;


#endif // _SPELLCHECK_H